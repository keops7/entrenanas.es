{
  "name": "entrenanas",
  "version": "0.1.0",
  "private": true,
  "description": "Sitio web entrenanas.es",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=entrenanas",
    "deploy:preview": "wrangler pages deploy public --project-name=entrenanas --branch=preview"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
